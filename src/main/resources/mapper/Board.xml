<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.board.mapper.BoardMapper">

	<select id="selectAll" resultType="Board">
		select * from board
	</select>

	<select id="selectOne" parameterType="String" resultType="Board">
		select * from board where board_id = #{board_id}
	</select>

	<insert id="insert" parameterType="Board">
		insert into board(board_id, title, content, date,userid,count,like_count) values (#{board_id}, #{title}, #{content},sysdate(),"ssafy", 0,0)
	</insert>

	<delete id="delete" parameterType="String">
		delete from board where board_id = #{board_id}
	</delete>

	<update id="countUp" parameterType="string">
		update board set count = count+1 where board_id = #{board_id}
	</update>

	<update id="likeUp" parameterType="string">
		update board set like_count = like_count+1 where board_id = #{board_id}
	</update>

	<update id="update" parameterType="Board">
		update board set title = #{title}, content = #{content}, date = sysdate(), userid = "ssafy",count = 0, like_count = 0 where board_id = #{board_id}
	</update>

<!--	<select id="search" parameterType="String" resultType="Board">-->
<!--		select * from board-->
<!--		<if test="content != null">-->
<!--			where content like concat("%", #{content}, "%")-->
<!--		</if>-->
<!--	</select>-->
</mapper>